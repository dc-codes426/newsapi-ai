# newsapi-ai
An AI-enabled chatbot that provides all your news searching needs.

Talk to the chatbot in nautral language. The agent will automatically determine the optimal NewsAPI query parameters based on your input. The agent can chat back, or return the results in json format. Alternatively, if you already have the query you want, you can submit that in json format.

## Roadmap
- [ ] result dataset schema

